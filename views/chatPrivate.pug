.ui.container
    #emergency-group(hidden)
    #emergency-group-initiator(hidden)
    button.btn-confirm#closeEmgergencyGroup(onclick='closeGroupChat()', hidden) CLOSE
    .ui.comments.chatRoomComments
        #privateDialog
    .ui#inputBox
        .ui.input.chatInput#privateInputText
            input#privateInputContent(type="text" placeholder="Message..." onkeydown="oneKeyPress(event)")
        #privateSendBtn.ui.submit.button(onclick="sendMessage(false)") Send

