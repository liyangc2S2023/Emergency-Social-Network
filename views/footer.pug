div.main-footer
    div.ui.five.item.menu#menu
        a.item.menuItem(class = (pageView == "Directory" ? "active" : "") id = "directoryMenu" onclick="displayDirectory()")
            i.address.book.outline.icon
            br
            p Directory
        a.item.menuItem(class = (pageView == "Public" ? "active" : "") id = "publicMenu" onclick="displayPublic()")
            i.wechat.icon
            p Public
        a.item.menuItem(class = (pageView == "Announcement" ? "active" : "") id = "announcementMenu" onclick="displayAnnouncement()")
            i.clipboard.icon
            br
            p Announce
        a.item.menuItem(class = (pageView == "status" ? "active" : "") id = "statusMenu" onclick="displayStatus()")
            i.compass.icon
            p Status
        a.item.menuItem(class = (pageView == "Discover" ? "active" : "") id = "discoverMenu" onclick="displayDiscover()")
            i.compass.icon
            p Discover
